# Commit Master Linux x64 clipboard helper

This package is an optional, platform-specific dependency of `commit-master`.
It contains a native executable built from `native/linux-file-clipboard` during
release. It is not intended to be installed or invoked separately.
